/*import com.skqtec.repository.WebSocketsRepository;
import org.springframework.stereotype.Repository;

import java.util.ArrayList;
import java.util.List;

i*mport com.skqtec.repository.WebSocketsRepository;
        /package com.skqtec.repository;

@Repository
public class WebSocketsRepositoryImpl implements WebSocketsRepository {

    private List<Websockets>socketsPools = new ArrayList<Websockets>();

    public boolean addWebSocket(Websockets websocket) {
        return socketsPools.add(websocket);
    }

    public List<Websockets> getWebSocket(){
        return socketsPools;
    }

    public boolean deleteWebSockets(Websockets websocket) {
        return socketsPools.remove(websocket);
    }
}
*/